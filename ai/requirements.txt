# DRISHTI ai/ module (Engineer 2) - Python dependencies
# Install with: pip install -r requirements.txt --break-system-packages
#
# These should also be merged into the project's root requirements.txt by
# Engineer 1 alongside the other engineers' dependencies - see
# ARCHITECTURE.md Section 7 (Technology Stack).

numpy>=1.24
rasterio>=1.3
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
scipy>=1.11
torch>=2.1          # only required for LightUNet training/inference; the
                     # ThresholdFloodModel baseline works without it

# Optional - only needed if using `--upload` / storage_utils.py
supabase>=2.0
